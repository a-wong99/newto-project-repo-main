Files labelled Bookstore_Montly_Sales_2024_xx.csv should be unioned in the data model.

Then a relationship must be created between the unioned table and the files labelled Bookstore_Sales.csv and Publisher_Info.csv.

Below is an image demonstrating what the data model should look like:

![DataModel]()
